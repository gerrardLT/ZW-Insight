# 集成测试配置 — 直连服务器联调库
spring:
  datasource:
    url: jdbc:mysql://129.204.3.200:3306/zw_insight?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4
    username: root
    password: zwinsight123
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 129.204.3.200
      port: 6379
      password: ''
      database: 0
  # 禁用 Flyway（集成测试不执行迁移）
  flyway:
    enabled: false

  # 禁用不需要的自动配置
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration

# MyBatis-Plus
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 日志
logging:
  level:
    com.zwinsight: DEBUG
    org.springframework.jdbc: DEBUG
