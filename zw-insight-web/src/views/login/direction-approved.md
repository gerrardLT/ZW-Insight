# Direction Approved · 登录页视觉优化（非视频部分）

## 选定方向（沿用，非新方向）
视频背景（整张铺满）+ 左侧品牌文案 + 右侧白底登录卡。该方向由用户在多轮迭代中选定并确认：
- 用户曾要求"整张背景、不要左右分栏"→ 后又要求恢复左侧文案 → 最终确认"视频背景 + 左文案 + 右卡"。
- 用户否决过浮夸的"铭牌/HUD"层（原话："你tm给我改回来 太浮夸了"），要求"在原来的基础上加上一些小细节"。

## 本次任务（迭代，豁免三方向门）
用户原话（/huashu-design）："针对当前登录页 除了视频背景外 我需要进行视觉优化"。
属于**已选定方向后的迭代**（同一项目内改稿），按 skill「唯一豁免」第 2 条不重跑三方向门。

## 迭代范围（仅非视频部分）
左文案排版/签名、卡片材质层次、Tab 指示条动效、输入光标/选区、特性项微交互、滑块成功勾绘制、版权签名。全部克制、不改变结构与配色体系。

## 用户选定（2026-09-24）
用户原话："按照 B方案 对当前登录页进行优化升级 除了背景还是用原来的视频"。
→ 选定 **方向 B · 精密仪器 Precision**（login-prototypes/round3/b-precision.html），已落地到 index.vue；背景保留视频 login-bg-web.mp4。
三版初稿：round3/a-editorial.html、b-precision.html、c-darkglass.html（截图 a/b/c.png 本地）。

## 记录
- 豁免依据：skill Fallback「唯一豁免」#2（已选定方向后的迭代）。
- 记录时间：2026-09-24。
